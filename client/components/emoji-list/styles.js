import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    padding: 5,
  },
  emoji_wrapper: {
    marginRight: 5,
    padding: 2,
  },
  emoji: {
    fontSize: 21,
  },
});

export default styles;
